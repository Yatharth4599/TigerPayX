{
  "buildCommand": "prisma generate && (prisma migrate deploy || echo 'Migration skipped - DATABASE_URL not set') && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}

