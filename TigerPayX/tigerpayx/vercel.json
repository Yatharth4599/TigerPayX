{
  "buildCommand": "prisma generate && (node scripts/resolve-migration.js || echo 'Migration resolution skipped') && (prisma migrate deploy || echo 'Migration skipped - DATABASE_URL not set') && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}

